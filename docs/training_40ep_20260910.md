# robot-s0 40-episode PI0.5 training — 2026-09-10

Superseded: stopped at the user's request at 2026-09-10 23:34 China time,
after optimizer step 151. The replacement uses one complete trajectory per
source episode, with pauses compacted: see [the replacement run](training_40merged_20260910.md).
The remaining text records the stopped run, not the current training.

Started on robot-s1 at 2026-09-10 23:15 China time, using the existing
`rlinf-pnp7:ffmpeg` image and the same `RLinf-Pi05-LIBERO-SFT` initial weights
as the previous PNP7 training. This is a fresh LoRA run on the new data, not
a resume from the previous 30-episode PNP7 checkpoint.

Startup verification: reached optimizer step 7/7,500 with finite loss and
gradient norm (step 7 loss 5.04, still in warmup), no traceback, CUDA OOM or
NaN loss. Observed step times after initialization were approximately 7–8 s.
The container was running at handoff; these early measurements do not establish
convergence or task success.

## Data and compatibility

- Source: robot-s0 `/home/liushuai/workspace/pnp7-lead-teleop/episodes`.
- 40 complete source episodes, 16,026 aligned source frames.
- Failed directories `ep009`, `ep027`, `ep028` were excluded; source data was not modified.
- Copied to robot-s1 `/home/andyls/vla/pnp7/raw40_20260910`.
- The SHA-256 digest of the sorted per-file checksum listing matched on both hosts:
  `935cc8610eb3d6012d3edc4f667a8b096e25e77b04cbaecf4eb7f6c642acb8f9`.
- New camera field `rgb_cam2022` is mapped to the existing external-camera input.
- New Robotiq field `gripper_requested_raw` is mapped to the existing binary
  convention: register <128 gives 1=open, otherwise 0=closed. The legacy
  metre-valued `gripper_command` is -1 throughout this dataset and is not used.
- State remains 10D, action remains 7D measured end-effector transition plus
  absolute binary gripper; model padding remains 32D and horizon remains 10.
- User confirmed the prompt: `pick up the blue cube and place it in the plate`.

This initial run split four gaps over 200 ms using the converter's segmentation
support, based on the observed movement across pauses. The user subsequently
clarified that each source episode should stay intact. Minimum segment size was 11 source
frames so the short 21-frame segment is retained. The resulting dataset has
44 continuous segments and 15,982 labelled frames; no source segment was
dropped. Every retained action is within a continuous segment.

Dataset: `/home/andyls/vla/pnp7/lerobot/pnp7_40ep_20260910`.
The converted dataset retains 40 close transitions and 40 open transitions.
Normalization was recomputed from all new states and all 10-step action chunks,
with the same last-action repetition used by the installed LeRobot loader.

## Training

| Setting | Value |
|---|---|
| Container | `pnp7_pi05_40ep_20260910` |
| Image | `rlinf-pnp7:ffmpeg`, ID prefix `33ef89184f7a` |
| RLinf commit | `0505431899574619da86f551bad70b71e0ea2177` plus existing working-tree customizations |
| Configuration | `examples/sft/config/pnp7_sft_pi05_40ep_20260910.yaml` |
| Initial model directory | `/home/andyls/vla/models/pi05-pnp7-40ep-20260910` |
| Model config | `pi05_pnp7` |
| LoRA rank / batch size | 32 / 8 |
| Learning rate | 2.5e-5 |
| Optimizer steps / warmup | 7,500 / 250 |
| Checkpoint interval | 2,500 steps |

The new model directory's safetensors file is a hard link to the same original
base weights. Its `pnp7/teleop/norm_stats.json` is newly computed and independent
of the old runs. All previous datasets and checkpoints are retained.

Log root on robot-s1:

```
/home/andyls/vla/RLinf/logs/20260910-15:15:00-pnp7_sft_pi05_40ep_20260910
```

Expected final checkpoint, after successful completion:

```
<log root>/pnp7_sft_pi05_40ep_20260910/checkpoints/global_step_7500/actor/model_state_dict/full_weights.pt
```

From the Mac, follow the live log:

```bash
ssh robot-s1 'docker logs --tail 40 -f pnp7_pi05_40ep_20260910'
```

Inspect process status:

```bash
ssh robot-s1 'docker inspect pnp7_pi05_40ep_20260910 --format "{{.State.Status}} exit={{.State.ExitCode}}"'
```

## Verification and reproducibility

Run artifacts on robot-s1: `/home/andyls/vla/pnp7/runs/20260910_40ep`.
This directory contains the manifest, configuration snapshot, input hashes,
conversion log, dataset validation, loader smoke test log and launch script.

The isolated converter is
`/home/andyls/vla/pnp7/tools/convert_to_lerobot_robot_s0_20260910.py`;
the previous converter was not overwritten. Regression checks compared all
15,986 candidate within-source labels against raw Robotiq requests and
end-effector poses before removing the four cross-gap labels. The final
parquet validation checks shape, finite values, counts and gripper transitions.
The real OpenPI loader smoke test checks both cameras, 32D padding, 10-step
chunks, episode-end repetition and the new model's normalization path.

Training completion and real-robot task success require separate verification.
The previous inference controller also needs a Robotiq deployment adapter
before this checkpoint can be executed on robot-s0.
