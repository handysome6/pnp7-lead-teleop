"""Shared code for PNP-7 lead-arm teleoperation.

Only the lead-arm driver lives here so far. Everything else in this repo is a
script with a `main()`, not a library: the scripts import from this package,
never from each other.
"""
